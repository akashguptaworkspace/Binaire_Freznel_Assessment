{
  "name": "binaire-freznel-assessment-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-user CSV all-reduce queueing system — Node.js server (Express + worker_threads) with a binary-heap priority queue, deadlock guard, and SSE live status.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "gen:samples": "node scripts/generate-samples.mjs",
    "simulate": "node scripts/simulate-clients.mjs"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^2.0.1"
  }
}
