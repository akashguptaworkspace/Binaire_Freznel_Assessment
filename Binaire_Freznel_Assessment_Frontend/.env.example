# The deployed backend base URL (no trailing slash). On Vercel, set this as a
# Project Environment Variable and redeploy.
VITE_API_BASE=https://freznel-queue-backend.onrender.com

# You can also override at runtime without rebuilding:
#   https://your-frontend.vercel.app/?api=https://your-backend
# (the value is stored in localStorage and reused)
